import React, { useState } from 'react'; // useStateをインポート

const ChatScreen = () => {
  const [messages, setMessages] = useState([]); // 例: メッセージの状態管理

  // コンポーネントのロジック...

  return <div>トーク一覧</div>;
};

export default ChatScreen;